/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: 'out',
    output: 'export',
    // basePath: '/home',
    // experimental: {
    //     reactCompiler: true,
    // },
};

export default nextConfig;
